import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
	return (
		<>
			<Head>
				<title>Ion is running</title>
			</Head>
		</>
	);
};

export default Home;
